var es = require('event-stream');
module.exports = function(app) {
  // var TestResult = app.models.TestResult;
  // TestResult.createChangeStream(function(err, changes) {
  //   changes.pipe(es.stringify()).pipe(process.stdout);
  //});

}
